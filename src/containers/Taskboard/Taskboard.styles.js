const styles = () => {
  return {
    box: {
      display: 'flex',
      alignItems: 'center',
    },
    shape: {
      padding: 20,
      margin: 10,
      backgroundColor: 'blue',
    },
  };
};

export default styles;
